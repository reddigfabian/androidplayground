<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="fileIOViewModel"
            type="com.fabian.androidplayground.ui.main.fileio.list.viewmodels.FileIOViewModel"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:padding="8dp">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/fileRecyclerView"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@id/openFileButton"
            android:padding="16dp"
            android:foreground="@{fileIOViewModel.empty ? @drawable/ic_launcher_foreground : null}"
            android:background="@drawable/image_placeholder_bg"
            android:overScrollMode="never"
            android:foregroundGravity="center"
            tools:foreground="@drawable/ic_launcher_foreground"/>

        <Button
            android:id="@+id/openFileButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minWidth="160dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/nextButton"
            app:layout_constraintTop_toBottomOf="@id/fileRecyclerView"
            app:layout_constraintBottom_toBottomOf="parent"
            android:text="@string/open_file"
            android:layout_marginTop="8dp"
            android:enabled="@{fileIOViewModel.empty}"/>

        <Button
            android:id="@+id/nextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minWidth="160dp"
            app:layout_constraintStart_toEndOf="@id/openFileButton"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/fileRecyclerView"
            app:layout_constraintBottom_toBottomOf="parent"
            android:text="@string/next"
            android:layout_marginTop="8dp"
            android:enabled="@{!fileIOViewModel.empty}"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>